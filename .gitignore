# Node
node_modules/
dist/
dist-kontrol/
npm-debug.log*
.env
.env.local

# Firebase
.firebase/
firebase-debug.log*
firestore-debug.log*
ui-debug.log*

# Terraform (Sovereign Exclusion)
.terraform/
*.tfstate
*.tfstate.backup
.terraform.lock.hcl

# Secrets & Keys (TS-Λ3 Protection)
secrets/
rpr-myaudit-admin.json
github-ci-key.json

# Standard JSON Configs (EXCEPTIONS)
!package.json
!package-lock.json
!tsconfig.json
!firestore.indexes.json
!firestore.rules.json
!firebase.json
!.firebaserc

# OS
.DS_Store